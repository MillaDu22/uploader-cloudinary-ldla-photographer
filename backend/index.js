const express = require('express');
const app = express();
//const CloudinaryServer = require('ldla-server-uploader-cloudinary');

app.listen(3001, () => {
    console.log('Server running on port 3001');
});